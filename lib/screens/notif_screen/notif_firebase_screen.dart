import 'package:flutter/material.dart';

class NotifFirebaseScreen extends StatelessWidget {
  const NotifFirebaseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column()),
    );
  }
}
